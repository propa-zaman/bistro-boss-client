import React from 'react';
import useTitle from '../../../hook/useTitle';

const MyCart = () => {
    useTitle('My Cart | Dashboard')
    return (
        <div>
            
        </div>
    );
};

export default MyCart;